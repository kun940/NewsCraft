# NewsCraft 部署文档

> 面向实际部署与运维的操作指南：覆盖 **Docker 一键部署**（推荐）与 **本地手动部署** 两条路径，含环境要求、配置变量说明、数据库初始化、验证清单、升级备份与故障排查。
> 配套阅读：`docs/Docker部署指南.md`（Dockerfile / compose 原理逐段拆解，教学向）、`README.md`（项目总览）。

---

## 目录

- [一、部署方式选择](#一部署方式选择)
- [二、环境要求](#二环境要求)
- [三、部署前置：Ollama 本地模型（AI 功能依赖）](#三部署前置ollama-本地模型ai-功能依赖)
- [四、Docker 一键部署（推荐）](#四docker-一键部署推荐)
- [五、本地手动部署](#五本地手动部署)
- [六、配置说明](#六配置说明)
- [七、数据库初始化](#七数据库初始化)
- [八、存量数据 AI 加工](#八存量数据-ai-加工)
- [九、部署验证清单](#九部署验证清单)
- [十、升级、备份与清理](#十升级备份与清理)
- [十一、常见问题排查](#十一常见问题排查)

---

## 一、部署方式选择

| 维度 | Docker 部署（推荐） | 本地手动部署 |
| --- | --- | --- |
| 依赖 | 仅需 Docker + Docker Compose | Python 3.13 / Node 20 / MySQL / Redis / Ollama 全要自己装 |
| 数据库 | MySQL、Redis 由 compose 自动拉起，建表脚本自动执行 | 手动装并初始化 |
| 一致性 | 镜像打包运行环境，换机器结果一致 | 受本机环境差异影响 |
| 适用场景 | 交付演示、生产运行、换机器复现 | 日常开发、改代码调试 |

> 两条路径都需要宿主机安装 **Ollama**（AI 向量化 / 推荐 / 问答依赖），Docker 部署时后端容器通过 `host.docker.internal:11434` 访问宿主机 Ollama。

---

## 二、环境要求

### 2.1 Docker 部署

| 依赖 | 版本 | 说明 |
| --- | --- | --- |
| Docker Engine | 20.10+ | 含 `docker compose` 插件（v2） |
| Docker Desktop（Windows/macOS） | 最新 | Windows 建议启用 WSL2 后端 |
| Ollama | 最新 | 宿主机安装，用于提供 bge-m3 / qwen2.5 模型 |
| 磁盘 | ≥ 10GB 可用 | 镜像（约 2GB）+ MySQL 数据卷 + 向量库 |

### 2.2 本地手动部署

| 依赖 | 版本 | 说明 |
| --- | --- | --- |
| Python | 3.13 | 后端运行时（`pyproject.toml` 要求 `>=3.13`） |
| uv | 最新 | 依赖安装（也可用 pip 替代） |
| Node.js | 20+ | 前端构建 |
| MySQL | 8.0 | 业务数据库，utf8mb4 |
| Redis | 7.x | 缓存（db0）+ Arq 任务队列（db1） |
| Ollama | 最新 | bge-m3（Embedding）+ qwen2.5（LLM） |

---

## 三、部署前置：Ollama 本地模型（AI 功能依赖）

AI 能力（新闻向量化、个性化推荐、RAG 问答）依赖宿主机 Ollama 提供模型。**不安装 Ollama 时系统仍可部署运行，传统新闻浏览功能不受影响，仅 AI 接口不可用**。

```powershell
# 1. 安装 Ollama（https://ollama.com/download）

# 2. 拉取模型（bge-m3 约 1.2GB，qwen2.5:7b 约 4.7GB）
ollama pull bge-m3
ollama pull qwen2.5:7b
ollama list        # 确认两个模型都已就绪

# 3. 验证服务可达
curl.exe http://localhost:11434/api/tags
```

> **切云端模型（可选）**：不想本地跑大模型时，可改 `后端代码/backend/config/config.yaml` 的 `llm` 段指向 DeepSeek / 通义等 OpenAI 兼容 API（`provider / api_key / base_url / chat_model`），Embedding 同理切换。改配置即可，代码零改动。

---

## 四、Docker 一键部署（推荐）

### 4.1 编排内容

`docker-compose.yml` 共编排 5 个服务：

| 服务 | 镜像 | 端口 | 职责 |
| --- | --- | --- | --- |
| `mysql` | mysql:8.0 | 内部 3306 | 业务库，首次启动自动执行建表脚本 |
| `redis` | redis:7-alpine | 内部 6379 | 缓存 + Arq 任务队列 |
| `backend-api` | newscraft-backend（自建） | **8000** | FastAPI 服务 |
| `backend-worker` | newscraft-backend（同镜像） | — | Arq 异步任务 Worker（向量化/摘要/兴趣同步） |
| `frontend` | newscraft-frontend（自建） | **8080** | nginx 托管 Vue 产物，/api 反代到后端 |

数据卷：`mysql_data`、`redis_data`、`chroma_data`（删除容器不丢数据）。

### 4.2 部署步骤

```powershell
# ① 进入项目根目录
cd "D:\learning programs\NewsCraft"

# ② 准备环境变量
Copy-Item .env.example .env
#    编辑 .env，必填 3 项：
#      MYSQL_PASSWORD   → 数据库 root 密码（任意自设）
#      SECRET_KEY       → Token 签名密钥，建议随机串：
#                          python -c "import uuid;print(uuid.uuid4().hex)"
#      DEEPSEEK_API_KEY → 大模型 API Key（config.yaml 默认 provider 是 deepseek 时必填；
#                          若按第三节切到本地 ollama 可留占位）

# ③ 构建并启动
docker compose up -d --build        # 首次构建需拉基础镜像 + 装依赖，约 5-10 分钟

# ④ 查看状态与日志
docker compose ps                    # 5 个服务均应为 Up（healthy）
docker compose logs -f backend-api   # 跟踪后端日志（Ctrl+C 退出）
```

### 4.3 访问入口

| 入口 | 地址 |
| --- | --- |
| 前端页面 | http://localhost:8080 |
| 后端接口文档（Swagger） | http://localhost:8000/docs |
| 后端健康冒烟 | http://localhost:8000/api/news/categories |

### 4.4 常用运维命令

```powershell
# 重启某个服务（如改了后端代码）
docker compose up -d --build backend-api

# 查看所有容器状态
docker compose ps

# 进入容器调试
docker compose exec backend-api bash

# 查看 worker 日志（AI 加工任务在这里执行）
docker compose logs -f backend-worker

# 停止全部（数据保留）
docker compose down

# 停止并清空数据卷（数据库/向量库全部重置，慎用！）
docker compose down -v
```

> **注意**：`backend-api` 与 `backend-worker` 必须同时运行。AI 加工接口只负责「受理入队」，真正的向量化/摘要由 worker 消费执行；worker 挂了新闻会一直停在 pending。

---

## 五、本地手动部署

### 5.1 后端

```powershell
# ① 依赖安装（项目根目录）
cd "D:\learning programs\NewsCraft"
Copy-Item .env.example .env          # 填 MYSQL_PASSWORD / SECRET_KEY / DEEPSEEK_API_KEY
uv sync --extra ai                   # 安装全部依赖（含 ai 可选组：langchain/chroma/arq 等）
# 无 uv 时用 pip：.venv\Scripts\python.exe -m pip install -r requirements.txt 加 ai 组

# ② 确认基础服务已启动
#    MySQL 8（本机 3306）、Redis（本机 6379）、Ollama（本机 11434）

# ③ 初始化数据库（见第七节）

# ④ 启动 API（终端 1，在 backend 目录下）
cd 后端代码\backend
..\..\.venv\Scripts\python.exe -m uvicorn main:app --reload --port 8000

# ⑤ 启动异步 Worker（终端 2，同样在 backend 目录下，保持运行）
cd 后端代码\backend
..\..\.venv\Scripts\python.exe -m arq tasks.worker.WorkerSettings
```

### 5.2 前端

```powershell
cd 03-前端项目代码\xwzx-news
npm install

# 开发模式（热更新，默认 5173，已配置后端跨域）
npm run dev

# 生产构建（产物在 dist/，可交给任意静态服务器/nginx）
npm run build
npm run preview
```

### 5.3 新闻爬虫（可选组件）

```powershell
cd 爬虫代码
# 依赖：复用项目根目录虚拟环境
..\.venv\Scripts\python.exe -m pip install -r requirements.txt

# 运行（自动复用根目录 .env 的 MYSQL_PASSWORD 连接数据库）
..\.venv\Scripts\python.exe main.py                    # 默认网易源
..\.venv\Scripts\python.exe main.py --source chinanews # 切换中新网 RSS
..\.venv\Scripts\python.exe main.py --limit 10         # 本次最多 10 条
```

接入定时调度（Windows 任务计划程序示例，每小时跑一次）：

```powershell
schtasks /create /tn NewsCrawler /tr "cmd /c cd /d D:\learning programs\NewsCraft\爬虫代码 && ..\.venv\Scripts\python.exe main.py" /sc hourly
```

---

## 六、配置说明

### 6.1 配置体系

配置采用 **config.yaml 定结构 + .env 管密钥** 的双层机制（`config/settings.py` 加载）：

- `后端代码/backend/config/config.yaml`：全部可调参数（结构默认值）
- 项目根目录 `.env`：敏感值，通过 `${VAR}` 占位符注入 yaml
- 环境变量可覆盖任意嵌套配置：`DATABASE__HOST` = `settings.database.host`（Docker 部署正是用这种方式把 `localhost` 指向容器服务名）

优先级：**init 参数 > 系统环境变量 > .env > config.yaml**。

### 6.2 环境变量总表（.env）

| 变量 | 必填 | 说明 |
| --- | --- | --- |
| `MYSQL_PASSWORD` | ✅ | MySQL root 密码（对应 `database.password`） |
| `SECRET_KEY` | ✅ | Token 签名密钥（对应 `auth.secret_key`），用 `uuid.uuid4().hex` 生成 |
| `DEEPSEEK_API_KEY` | ✅ | 大模型 API Key（对应 `llm.api_key`；config.yaml 默认 provider=deepseek） |
| `REDIS_PASSWORD` | — | Redis 密码；本机未开 requirepass 时留空 |
| `CHROMA_PERSIST_DIR` | — | 向量库持久化目录；Docker 部署由 compose 覆盖为 `/data/chroma`，无需设置 |

### 6.3 config.yaml 关键配置（docker-compose 覆盖项）

Docker 部署时 compose 用环境变量覆盖了这些值，本地部署则需要保证本机服务与配置一致：

| 配置项 | 本地默认 | Docker 覆盖 | 说明 |
| --- | --- | --- | --- |
| `database.host` | localhost | mysql | 容器内服务名即 DNS |
| `redis.host` | localhost | redis | 同上 |
| `tasks.arq_redis_url` | redis://localhost:6379/1 | redis://redis:6379/1 | Arq 队列（db1，与业务缓存 db0 隔离） |
| `embedding.base_url` | http://localhost:11434 | http://host.docker.internal:11434 | 容器内访问宿主机 Ollama |
| `vector_db.persist_directory` | ${CHROMA_PERSIST_DIR} | /data/chroma | 挂载到 chroma_data 卷 |
| `app.env` | dev | prod | 运行环境 |

### 6.4 AI 模型配置（config.yaml `llm` 段）

```yaml
llm:
  provider: "deepseek"        # ollama(本地) / deepseek / dashscope(通义) / openai
  api_key: "${DEEPSEEK_API_KEY}"
  base_url: "https://api.deepseek.com"
  chat_model: "deepseek-v4-flash"
  timeout: 60
  max_retries: 3

embedding:
  provider: "ollama"
  model: "bge-m3"             # 1024 维
  base_url: "http://localhost:11434"
```

> 切换模型只改配置文件，业务代码零改动（`core/rag_core/models.py` 模型工厂统一分发）。**注意**：Embedding 模型（bge-m3）变更维度后，Chroma 内旧向量不可比，需全量重向量化。

---

## 七、数据库初始化

### 7.1 初始化脚本

| 脚本 | 作用 |
| --- | --- |
| `scripts/migration/database.sql` | 建库 `newscraft_app` + 8 张业务表 |
| `scripts/migration/database_rag_upgrade.sql` | AI 预留字段 + `ai_chat_record` + `news_vector_log`（幂等，可重复执行） |

### 7.2 执行方式

- **Docker 部署**：无需手动执行。compose 将 `./scripts/migration` 挂载到 MySQL 容器的 `/docker-entrypoint-initdb.d`，**首次初始化数据卷时自动执行**；
- **本地部署**：手动执行：

```powershell
cd "D:\learning programs\NewsCraft"
mysql -u root -p < scripts\migration\database.sql
mysql -u root -p < scripts\migration\database_rag_upgrade.sql
```

> ⚠️ MySQL 的 `/docker-entrypoint-initdb.d` 只在数据卷为空时执行一次。改了 SQL 想重跑，需 `docker compose down -v` 清卷后重建（会清空全部数据）。

### 7.3 验证建表

```sql
USE newscraft_app;
SHOW TABLES;
-- 期望：user / user_token / news_category / news / related_news / favorite / history / ai_chat / ai_chat_record / news_vector_log
```

---

## 八、存量数据 AI 加工

新闻的 AI 加工（向量化 + 摘要）由爬虫入库时**自动触发**；对存量历史数据，需要手动调用接口补做一次。

```powershell
# ① 注册/登录拿 token（返回体 data.token）
curl.exe -X POST http://localhost:8000/api/user/register -H "Content-Type: application/json" -d "{\"username\":\"deploy\",\"password\":\"test123\"}"

# ② 全量向量化（body 为空 = 处理所有未向量化新闻；接口只受理入队，worker 异步执行）
curl.exe -X POST http://localhost:8000/api/ai/news/vector -H "Content-Type: application/json" -H "Authorization: Bearer <TOKEN>" -d "{}"

# ③ 全量摘要（同理，缺省处理所有 ai_summary 为空的新闻）
curl.exe -X POST http://localhost:8000/api/ai/news/summary -H "Content-Type: application/json" -H "Authorization: Bearer <TOKEN>" -d "{}"
```

**观察执行情况**（worker 日志 + 任务日志表）：

```sql
SELECT task_type, task_status, COUNT(*) FROM news_vector_log GROUP BY task_type, task_status;
-- 期望：vectorize/success、summary/success 有行，failed 为 0 或少量可解释失败
```

---

## 九、部署验证清单

部署完成后逐项确认：

| # | 检查项 | 命令/操作 | 期望 |
| --- | --- | --- | --- |
| 1 | 容器状态 | `docker compose ps` | 5 个服务 Up（mysql/redis healthy） |
| 2 | 前端页面 | 浏览器打开 http://localhost:8080 | 正常渲染新闻列表 |
| 3 | 后端接口 | 浏览器打开 http://localhost:8000/docs | Swagger 正常加载 |
| 4 | 分类接口 | `curl.exe http://localhost:8000/api/news/categories` | 返回 code=200 |
| 5 | 注册登录 | POST /api/user/register | 返回 token |
| 6 | AI 受理 | 带 token 调 /api/ai/news/vector（空 body） | acceptedCount>0，worker 日志逐条执行 |
| 7 | 加工回填 | `SELECT id, ai_summary, news_vector_id FROM news LIMIT 5` | 非空 |
| 8 | 任务日志 | `SELECT task_status, COUNT(*) FROM news_vector_log GROUP BY task_status` | success 占绝大多数 |
| 9 | Ollama 连通（容器内） | `docker compose exec backend-api python -c "import urllib.request;print(urllib.request.urlopen('http://host.docker.internal:11434/api/tags',timeout=5).status)"` | 输出 200 |
| 10 | 爬虫联动 | 运行一次爬虫，观察是否触发 AI 加工 | 日志出现「触发 AI 加工」 |

---

## 十、升级、备份与清理

### 10.1 代码升级

```powershell
git pull                     # 拉取最新代码
docker compose up -d --build # 重新构建受影响镜像并滚动重启
```

### 10.2 数据备份

```powershell
# MySQL 逻辑备份（Docker 部署）
docker compose exec mysql sh -c "mysqldump -uroot -p\$MYSQL_ROOT_PASSWORD newscraft_app" > backup_$(Get-Date -Format yyyyMMdd).sql

# 向量库备份：备份 chroma_data 卷（卷名前缀 = compose 项目名，先用 docker volume ls 确认）
docker volume ls   # 找到形如 <项目名>_chroma_data 的卷名
docker run --rm -v <项目名>_chroma_data:/data -v ${PWD}:/backup alpine tar czf /backup/chroma_$(Get-Date -Format yyyyMMdd).tar.gz -C /data .
```

### 10.3 清理

| 操作 | 命令 | 影响 |
| --- | --- | --- |
| 停服务保留数据 | `docker compose down` | 数据卷保留 |
| 停服务清数据 | `docker compose down -v` | **清空数据库/向量库** |
| 清理无用镜像 | `docker system prune -f` | 只清未使用镜像 |

---

## 十一、常见问题排查

| 现象 | 原因 | 解决 |
| --- | --- | --- |
| `docker compose up` 构建失败：中文路径报错 | Windows 下 Docker 对中文构建上下文兼容性问题 | 将项目复制到纯英文路径构建；或按报错调整 Dockerfile COPY 路径写法 |
| 后端连不上 MySQL | 容器内 `localhost` 指向容器自身 | compose 已用 `DATABASE__HOST: mysql` 覆盖；本地部署确认 MySQL 已启动、.env 密码正确 |
| 建表脚本没执行 | 数据卷已初始化过，initdb 只跑一次 | `docker compose down -v` 后重建（清数据） |
| 改了代码不生效 | 代码是构建时拷入镜像 | `docker compose up -d --build backend-api` / `frontend` |
| 前端 /api 502 | 后端未就绪或服务名不对 | `docker compose ps`、`docker compose logs backend-api`；nginx.conf 反代目标是 `backend-api:8000` |
| AI 接口 503 / worker 任务失败 | Ollama 未启动 / 模型未拉 | 宿主机 `ollama pull bge-m3`、`ollama pull qwen2.5:7b`；容器内验证 `host.docker.internal:11434` 可达 |
| worker 启动报 ImportError | 启动目录不对 | 必须在 `后端代码/backend` 目录下执行 `python -m arq tasks.worker.WorkerSettings` |
| 接口受理成功但 worker 不执行 | worker 没启动 / Redis 地址不一致 | 确认 worker 在运行；`tasks.arq_redis_url`（db1）前后端一致 |
| 向量维度不是 1024 | 模型拉错或换过模型 | `ollama list` 确认 bge-m3；换模型后需全量重向量化 |
| AI 接口 401 | token 缺失/过期 | 重新登录取新 token，请求头带 `Authorization: Bearer <TOKEN>` |
| 本地 5173 前端请求后端失败 | 跨域未放行 | main.py CORS 已允许 5173；Docker 部署走 nginx 同源反代无跨域 |

---

> 部署完成后，建议接着阅读《新闻爬取与加工》了解内容生产链路，或参考 `docs/` 下的模块教程进行二次开发。
